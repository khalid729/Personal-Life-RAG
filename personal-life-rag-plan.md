# المساعد الذكي — نظام إدارة الحياة الشخصية
## Personal Life Management System — Final Master Plan v2.0

---

## الرؤية
نظام ذكي شخصي يرتب الأفكار، المشاريع، المصاريف، والمواعيد — مصمم لشخص عنده أفكار كثيرة متشعبة ويحتاج نظام **يلحقه هو** مو العكس. النظام **استباقي** — يبادر ويتواصل ويتعلم من صاحبه.

---

## البنية التحتية

### الهاردوير
- **GPU**: NVIDIA RTX PRO 6000 Blackwell (96GB VRAM)
- **النموذج**: Qwen3-VL-32B-Instruct (90K context, ~94GB VRAM)

### البرمجيات الأساسية (Tech Stack)

| المكون | الأداة | الغرض |
|--------|--------|-------|
| **خدمة النموذج** | vLLM | Continuous Batching — نموذج واحد يعالج كل الطلبات بالتوازي |
| **النموذج** | Qwen3-VL-32B | رؤية + لغة + تحليل + ترجمة + استخراج |
| **الصوت** | WhisperX (Large-v3 Turbo) | صوت → نص + تمييز المتحدثين + timestamps |
| **قاعدة العلاقات** | FalkorDB | Knowledge Graph — 500x أسرع من Neo4j للـ AI |
| **البحث الدلالي** | Qdrant | Vector Search — فلترة متقدمة بالـ metadata |
| **الـ Embeddings** | BGE-M3 | متعدد اللغات (100+ لغة) — ممتاز للعربي والإنجليزي |
| **الباك إند** | FastAPI (async) | الـ API الرئيسي |
| **واجهة الويب** | Open WebUI | المحادثة من الكمبيوتر |
| **ربط Claude** | MCP Server | أدوات Claude Desktop |
| **الجوال** | Telegram Bot | أسرع إدخال (صوت + صور + نص) |
| **الذاكرة المؤقتة** | Redis | ملخص اليوم + آخر الرسائل + حالة المستخدم |
| **الجدولة** | APScheduler / Cron | المهام الاستباقية والتذكيرات |

---

## البنية الموجودة vs الجديدة (إيش شغال وإيش نضيف)

### الخدمات الشغالة حالياً (ISE 2026) — نستفيد منها مباشرة

| الخدمة | الحالة | البورت | التشغيل | الاستخدام في المشروع الجديد |
|--------|--------|--------|---------|----------------------------|
| **vLLM** | ✅ شغال | 8000 | `~/ise2026-rag/scripts/start_vllm.sh` | **نستخدمه كما هو** — نفس النموذج Qwen3-VL-32B, 90K context, FP8 KV Cache |
| **Open WebUI** | ✅ شغال (Docker) | 3000 | `docker start openwebui-ise2026` | **نستخدمه** — نضيف Tools جديدة بس |
| **Cloudflare Tunnel** | ✅ شغال (Docker) | — | `docker start cloudflared-ise2026` | **نستخدمه** — نضيف routes جديدة |
| **Nginx** | ✅ شغال | 8080 | config في `nginx.conf` | **نستخدمه** — نضيف locations جديدة |

### أوامر التشغيل الحالية:
```bash
# vLLM (موجود ومعد)
source /home/khalid/vllm-env/bin/activate
vllm serve Qwen/Qwen3-VL-32B-Instruct \
  --max-model-len 90000 \
  --gpu-memory-utilization 0.90 \
  --kv-cache-dtype fp8 \
  --attention-backend TRITON_ATTN \
  --trust-remote-code \
  --host 0.0.0.0 --port 8000

# Docker containers (موجودة)
docker start openwebui-ise2026 cloudflared-ise2026
```

### الروابط العامة الموجودة:
| الخدمة | الرابط |
|--------|--------|
| Open WebUI | https://khalidgpt.me |
| RAG API | https://api.khalidgpt.me |
| Neo4j Browser | https://neo4j.khalidgpt.me |
| MCP Server | https://mcp.khalidgpt.me |

---

### الخدمات اللي نوقفها (ISE 2026 — نستبدلها)

| الخدمة القديمة | السبب | البديل الجديد |
|----------------|-------|--------------|
| **Neo4j** (Docker: neo4j-ise2026, Port 7474/7687) | نستبدله بـ FalkorDB (500x أسرع) | FalkorDB |
| **ChromaDB** (Docker: chromadb-ise2026, Port 8100) | نستبدله بـ Qdrant (فلترة أقوى) | Qdrant + BGE-M3 |
| **Whisper Server** (Native, Port 8200) | نستبدله بـ WhisperX (أسرع + speakers) | WhisperX |
| **RAG API القديم** (Native, Port 8300) | نبني API جديد للنظام الشخصي | Personal RAG API |
| **MCP HTTP Server** (Native, Port 8400) | نبني MCP جديد بأدوات شخصية | Personal MCP Server |

### ملاحظة مهمة:
```
⚠️ لا توقف خدمات ISE 2026 إلا بعد ما النظام الجديد يكون جاهز
   الحل: شغّل الخدمات الجديدة على بورتات مختلفة أولاً
```

---

### الخدمات الجديدة (نركبها من الصفر)

| الخدمة | النوع | البورت المقترح | الملاحظة |
|--------|-------|---------------|----------|
| **FalkorDB** | Docker | 6379 | بديل Neo4j — Cypher-compatible |
| **Qdrant** | Docker | 6333 | بديل ChromaDB — REST + gRPC |
| **Redis** | Docker | 6380 | ذاكرة اليوم + Letta Memory + آخر الرسائل |
| **WhisperX** | Native | 8200 (نفسه) | بعد إيقاف Whisper القديم |
| **Personal RAG API** | Native | 8500 | API الجديد (يشتغل بالتوازي مع القديم أولاً) |
| **Personal MCP Server** | Native | 8600 | MCP جديد بأدوات شخصية |
| **Telegram Bot** | Native | — | لا يحتاج بورت (polling أو webhook) |
| **Scheduler** | Native | — | APScheduler يشتغل مع الـ API |
| **BGE-M3 Embeddings** | Native/API | — | يشتغل داخل الـ API process |

### أوامر تركيب الخدمات الجديدة:
```bash
# FalkorDB (بديل Neo4j)
docker run -d \
  --name falkordb-personal \
  -p 6379:6379 \
  -v /home/khalid/personal-rag/falkordb:/data \
  --restart unless-stopped \
  falkordb/falkordb:latest

# Qdrant (بديل ChromaDB)
docker run -d \
  --name qdrant-personal \
  -p 6333:6333 \
  -v /home/khalid/personal-rag/qdrant:/qdrant/storage \
  --restart unless-stopped \
  qdrant/qdrant:latest

# Redis (ذاكرة مؤقتة — جديد)
docker run -d \
  --name redis-personal \
  -p 6380:6379 \
  -v /home/khalid/personal-rag/redis:/data \
  --restart unless-stopped \
  redis:alpine

# WhisperX (بديل Whisper — بعد إيقاف القديم)
pip install whisperx
```

---

### خريطة البورتات الكاملة (بعد التركيب)

```
البورت    الخدمة              الحالة
═══════   ══════              ══════
3000      Open WebUI          ✅ موجود (يخدم النظامين)
6333      Qdrant              🆕 جديد
6379      FalkorDB            🆕 جديد
6380      Redis               🆕 جديد
8000      vLLM                ✅ موجود (يخدم النظامين)
8080      Nginx               ✅ موجود (نضيف routes)
8200      WhisperX            🔄 نستبدل Whisper
8500      Personal RAG API    🆕 جديد
8600      Personal MCP        🆕 جديد

يمكن إيقافها لاحقاً (بعد الانتهاء من ISE):
7474/7687 Neo4j               🔴 يتوقف
8100      ChromaDB            🔴 يتوقف
8300      ISE RAG API         🔴 يتوقف
8400      ISE MCP Server      🔴 يتوقف
```

---

### المجلدات على السيرفر

```
/home/khalid/
├── ise2026-rag/           ← المشروع القديم (يبقى يشتغل)
│   ├── scripts/
│   ├── docs/
│   ├── neo4j/data/
│   └── logs/
│
├── personal-rag/          ← المشروع الجديد
│   ├── scripts/
│   │   ├── rag_api.py     (API الجديد — Port 8500)
│   │   ├── mcp_server.py  (MCP الجديد — Port 8600)
│   │   ├── telegram_bot.py
│   │   ├── scheduler.py
│   │   └── whisperx_server.py
│   ├── prompts/           (برومبتات التصنيف والاستخراج)
│   ├── falkordb/data/
│   ├── qdrant/storage/
│   ├── redis/data/
│   ├── files/             (الملفات المرفوعة)
│   ├── logs/
│   └── docs/
│
└── vllm-env/              ← مشترك (نفس الـ vLLM للنظامين)
```

---

### استراتيجية الانتقال (Migration)

```
المرحلة 1: شغّل الخدمات الجديدة بالتوازي مع القديمة
           (بورتات مختلفة — ما يتعارضون)

المرحلة 2: وصّل Open WebUI بالـ API الجديد
           (أضف Tools جديدة — تشتغل جنب القديمة)

المرحلة 3: بعد ما تتأكد كل شي شغال
           → أوقف خدمات ISE القديمة
           → حرر البورتات
           → (اختياري) انقل بيانات ISE للنظام الجديد

المرحلة 4: (اختياري) نقل بيانات من Neo4j → FalkorDB
           لو تبي تحتفظ ببيانات المعرض
```

---

## القرارات المعمارية الأساسية

### 1. نموذج واحد — vLLM Continuous Batching

```
لا حاجة لنموذج ثاني. vLLM يعالج كل الطلبات بالتوازي:

  ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐
  │ رد  │ │تلخيص│ │استخراج│ │تذكير│  ← كلهم بنفس الوقت
  │ على │ │محادثة│ │حقائق │ │صباحي│     في batch واحد
  │المستخدم│ │     │ │      │ │     │
  └─────┘ └─────┘ └──────┘ └─────┘
           vLLM — Continuous Batching

  مو مثل Ollama (طلب → انتظار → طلب)
  هنا كل الطلبات تتعالج بالتوازي الكامل
```

**لماذا**: GPU واحد 96GB + vLLM batching = كافي لمستخدم واحد حتى مع 4-5 طلبات متزامنة.

### 2. تخزين موحد بالإنجليزية

```
المستخدم (عربي) ──→ النموذج يترجم ──→ يتخزن إنجليزي
                                              │
المستخدم (عربي) ←── النموذج يترجم ←── يبحث إنجليزي
```

**لماذا**:
- الإنجليزية أدق بالـ embeddings والبحث الدلالي
- أقل tokens (العربي يستهلك 1.5x-2x أكثر)
- أسهل للـ structured data والـ queries
- النص الأصلي بالعربي يُحفظ كـ metadata للرجوع

**مثال التخزين**:
```json
{
    "category": "home_maintenance",
    "reason": "AC maintenance",
    "amount": 500,
    "original_text_ar": "دفعت 500 ريال لأبو محمد عن صيانة المكيف"
}
```

### 3. ذاكرة بـ 3 طبقات (حل مشكلة الكونتكست)

```
┌─────────────────────────────────────────────────────┐
│  الطبقة 1: ذاكرة العمل (Redis)                       │
│  → آخر 3-5 رسائل (النص الكامل)                      │
│  → ~1,000-2,000 tokens                              │
│                                                      │
│  الطبقة 2: ذاكرة اليوم (Redis)                       │
│  → ملخص مضغوط لكل محادثات اليوم                     │
│  → حقائق وقرارات مو نص كامل                         │
│  → ~500-1,000 tokens                                │
│                                                      │
│  الطبقة 3: الذاكرة الدائمة (FalkorDB + Qdrant)      │
│  → كل شي مخزن بشكل دائم                             │
│  → تنجلب بس لما تنطلب (حسب السؤال)                  │
│  → ~2,000-5,000 tokens (متغير)                      │
└─────────────────────────────────────────────────────┘

الهدف: الكونتكست دايماً تحت 15K tokens (من أصل 90K)
= النموذج مرتاح ويرد بدقة وسرعة
```

**بعد كل محادثة** (بالتوازي مع أي طلب ثاني عبر vLLM):
1. استخراج حقائق بـ JSON → FalkorDB
2. تحديث ملخص اليوم → Redis
3. تخزين النص للبحث → Qdrant

**المحادثات المتقطعة**: ملخص اليوم يحافظ على السياق طول اليوم بدون ما يمتلي الكونتكست.

---

## الأنماط الذكية (AI Patterns)

### النمط 1: Contextual Retrieval — تحسين جودة البحث

**المشكلة**: RAG العادي يقطع النص chunks بدون سياق
**الحل**: قبل عمل embedding لكل chunk، النموذج يضيف سياق

```
قبل: "The budget was increased to 15,000 SAR"
بعد: "[Smart Home Project - Phase 2] The smart home automation
      budget was increased to 15,000 SAR for Zigbee hub and sensors"
```

**النتيجة**: فشل البحث ينزل 49% (مع reranking: 67%)

**التطبيق**:
```
ملف يترفع → يتقسم chunks → كل chunk + سياقه → النموذج يضيف سياق
→ الـ chunk المحسّن يتسوى له embedding بـ BGE-M3 → Qdrant
```

### النمط 2: Agentic RAG + Self-RAG — النظام يقرر ويصحح نفسه

**المشكلة**: RAG العادي نفس المسار كل مرة (بحث → نتيجة → رد)
**الحل**: Agent يفكر، يقرر وين يبحث، ويتحقق من النتيجة

```
سؤال المستخدم
      │
      ▼
┌──────────────┐
│  Think Step  │  "هذا سؤال مالي → أروح FalkorDB مباشرة"
└──────┬───────┘  "هذا بحث غامض → أروح Qdrant"
       │          "هذا عن علاقات → أستخدم GraphRAG"
       ▼
┌──────────────┐
│  Act Step    │  ينفذ البحث/الاستعلام المناسب
└──────┬───────┘
       ▼
┌──────────────┐
│  Reflect     │  "النتيجة كافية؟ صحيحة؟"
│  (Self-RAG)  │  "لا → أبحث بطريقة ثانية"
└──────┬───────┘  "إي → أضيف معلومات مفيدة وأرد"
       ▼
    الرد النهائي
```

**Smart Router** (بدون LLM — Python بس):
```
"كم صرفت؟"           → FalkorDB مباشرة (Cypher query)
"مين يطلبني؟"         → FalkorDB مباشرة
"وش عندي بكرة؟"       → FalkorDB مباشرة
"ذكرني أسوي كذا"      → FalkorDB (إضافة مباشرة)

"وش كنت أفكر فيه عن.." → Qdrant (بحث دلالي)
"وش القصة مع فلان؟"   → FalkorDB GraphRAG (تتبع علاقات)
"وريني كل شي عن..."   → Qdrant + FalkorDB (هجين)
```

### النمط 3: GraphRAG — تتبع العلاقات المعقدة

**المشكلة**: البحث بالـ vectors ما يفهم العلاقات الموزعة على عدة أماكن
**الحل**: يتتبع المسارات في الـ Graph بين الكيانات

```
سؤال: "وش العلاقة بين مشروع البيت الذكي وأبو محمد؟"

FalkorDB يتتبع:
  Smart Home ←─PAID_TO── Expense(500) ──PAID_TO─→ Abu Mohammed
  Smart Home ←─HAS_TASK── Wiring ──ASSIGNED_TO─→ Abu Mohammed
  Abu Mohammed ──HAS_ROLE──→ "Electrical Contractor"

النموذج يصيغ:
  "أبو محمد مقاول الكهرباء. دفعت له 500 صيانة مكيف.
   وعنده مهمة تمديدات الدور الثاني معلقة."
```

**النتيجة**: دقة 91% بدال 43% على الأسئلة المعقدة.
**الميزة**: FalkorDB مبني بالضبط لهالشي — 500x أسرع من Neo4j.

### النمط 4: Letta Memory — النظام يتعلم منك

**المشكلة**: كل محادثة النظام يبدأ من الصفر عن شخصيتك وتفضيلاتك
**الحل**: طبقة ذاكرة تتعلم وتتحدث تلقائي

```
Core Memory (دايماً في الكونتكست — ~200 tokens):
┌────────────────────────────────────────┐
│ • يفضل الرسائل القصيرة (3 نقاط max)   │
│ • أنشط الصبح (8-11)                   │
│ • يأجل مهام الصيانة كثير              │
│ • "بعدين" = لا تذكرني اليوم           │
│ • يحب الأرقام أول شي                  │
│ • يستخدم تلقرام أكثر من الويب         │
└────────────────────────────────────────┘

Recall Memory (تنجلب عند الحاجة):
┌────────────────────────────────────────┐
│ • آخر مرة ناقش مشروع X: 3 أيام        │
│ • يسأل عن أبو محمد كل أسبوع           │
│ • رفض فكرة Y مرتين                    │
└────────────────────────────────────────┘

التعلم التلقائي:
  أسبوع 1: ملخص صباحي 10 نقاط → ما يرد = طويل
  أسبوع 2: 5 نقاط → يرد "تمام" = أحسن
  أسبوع 3: 3 نقاط → يتفاعل = مثالي ✓
  → يحدّث Core Memory: "max 3 items in morning summary"
```

---

## الأنظمة الوظيفية (7 أنظمة)

### 1. نظام إدارة الملفات الذكي (Smart File Manager)

#### المسار التلقائي:
```
ملف يُرفع
  → تحديد النوع (MIME)
  → تصنيف ذكي (vLLM — بالتوازي مع أي طلب ثاني)
  → استخراج معلومات (JSON إنجليزي + النص الأصلي عربي)
  → Contextual Retrieval (إضافة سياق للـ chunks)
  → تخزين: FalkorDB (علاقات) + Qdrant (بحث) + Storage (ملفات)
  → إجراء تلقائي (لو فاتورة → مصروف، لو فيه تاريخ → تذكير)
```

#### أنواع التصنيف:
| النوع | الوصف | المعلومات المستخرجة (إنجليزي) |
|-------|-------|------------------------------|
| invoice | صورة أو PDF لفاتورة | amount, date, vendor, items |
| official_document | عقد، شهادة، وثيقة | type, date, parties, duration |
| personal_photo | صور ذكريات، أماكن | user_description, tags, location |
| info_image | سكرين شوت، سبورة | extracted_text, key_info |
| note | ملاحظة نص أو صوت | content, classification, topic |
| project_file | كود، تصميم، مخطط | linked_project, file_type |
| price_list | عروض أسعار | products, prices, validity |
| business_card | كرت شخصي | name, company, phone, email |

#### التعامل مع الأنواع:
- **صورة فيها معلومات** → vLLM Vision يقرأها → يستخرج → يخزن
- **صورة عادية** → المستخدم يوصفها → تتخزن مع الوصف والتاقات → تترجع لما يطلبها
- **PDF** → يسحب النص → Contextual Retrieval → Qdrant + يستخرج البيانات المهمة → FalkorDB
- **صوت** → WhisperX (نص + timestamps + متحدثين) → vLLM يفهم ويصنف → يتصرف تلقائي

---

### 2. النظام المالي (Financial Tracker)

#### الكيانات في FalkorDB:
```
Expense:
  - amount: float
  - currency: string (default: "SAR")
  - date: datetime
  - category: string (food, transport, bills, rent, maintenance...)
  - description: string (English)
  - original_text_ar: string (Arabic original)
  - paid_to: -> Person | Company
  - receipt_file: -> File (optional)
  - recurring: string (none/monthly/yearly)

Debt:
  - amount: float
  - direction: string ("owed_to_me" | "i_owe")
  - person: -> Person
  - reason: string (English)
  - date_created: datetime
  - date_due: datetime (optional)
  - status: string ("active" | "paid" | "partial")
  - partial_payments: [{ amount, date }]
```

#### الاستخدام:
| أنت تقول | النظام يسوي |
|----------|-------------|
| "دفعت 500 لأبو محمد عن صيانة" | Expense(500, "maintenance", paid_to: Abu Mohammed) |
| "فلان يطلبني 1000" | Debt(1000, "i_owe", person: X) |
| "أنا أطلب فلان 300" | Debt(300, "owed_to_me", person: X) |
| "سددت لفلان 200" | يعدل الدين (partial_payment + status update) |
| *ترفع صورة فاتورة* | vLLM Vision → يستخرج المبلغ والجهة → Expense |
| "كم صرفت هالشهر؟" | FalkorDB SUM query → ملخص بالتصنيفات |
| "مين يطلبني فلوس؟" | FalkorDB MATCH (d:Debt {direction: "i_owe", status: "active"}) |

#### التقارير المالية التلقائية:
- ملخص شهري (مصاريف حسب التصنيف)
- مقارنة مع الشهر اللي فات (% تغيير)
- تنبيه لو تصنيف معين زاد عن المعتاد
- ملخص الديون (اللي عليك واللي لك + المجموع)

---

### 3. نظام التذكيرات الذكية (Smart Reminders)

#### أنواع التذكيرات:
| النوع | المثال | السلوك |
|-------|--------|--------|
| once | "ذكرني أكلم المقاول السبت" | يذكرك مرة ويشيله |
| recurring | "ذكرني أدفع الإيجار أول كل شهر" | يتكرر تلقائي |
| persistent | "لا تنساني أجدد الاستمارة" | يفضل يذكرك لين تقول خلاص |
| event_based | "بعد ما أخلص المشروع، ذكرني..." | ينتظر الحدث |
| financial | "فلان يرجع لي فلوس نهاية الشهر" | يذكرك + يحدث الدين |

#### الكيان في FalkorDB:
```
Reminder:
  - content: string (English)
  - original_text_ar: string
  - type: string (once/recurring/persistent/event_based/financial)
  - priority: string (high/medium/low)
  - due_date: datetime
  - recurrence: string (daily/weekly/monthly/yearly)
  - linked_to: -> Project | Person | Topic
  - status: string (active/completed/snoozed)
  - snooze_count: int (للمتابعة — لو أجلته كثير يسألك)
  - created_at: datetime
```

---

### 4. نظام المشاريع والأفكار (Projects & Ideas Hub)

#### الكيانات في FalkorDB:
```
Project:
  - name: string
  - description: string (English)
  - status: string (idea/planning/active/paused/completed/abandoned)
  - priority: int (1-5)
  - domain: string (AI, IoT, Web, Business, Home...)
  - start_date: datetime
  - target_date: datetime
  - technologies: [string]
  - progress_percentage: int (0-100)
  - notes: [string]

Idea:
  - content: string (English)
  - original_text_ar: string
  - source: string (reading/conversation/thinking/browsing...)
  - status: string (raw/explored/linked_to_project/discarded)
  - linked_project: -> Project (optional)
  - tags: [string]
  - created_at: datetime

Task:
  - title: string
  - project: -> Project
  - status: string (todo/in_progress/done)
  - priority: string (high/medium/low)
  - due_date: datetime
  - estimated_time: string (optional)
  - completed_at: datetime (optional)
```

#### الميزات:
- "عندي فكرة عن..." → يحفظها → Qdrant يكتشف لو فيه فكرة مشابهة سابقة
- "وش المشاريع المتوقفة؟" → FalkorDB: MATCH (p:Project {status: "paused"})
- "حدث تقدم المشروع الفلاني" → يسجل ويعدل progress_percentage
- اكتشاف التكرار عبر Qdrant similarity search

---

### 5. نظام التخطيط اليومي (Daily Planner — ADHD Mode)

#### الفلسفة:
- **واقعي**: 3 إلى 5 مهام بالكثير (مو 50 مهمة تطفش)
- **مرن**: لو ما خلصت شي، ينتقل لبكرة بدون ذنب
- **ذكي**: يعرف الأهم بناءً على السياق والأولوية
- **محفز**: يبين إنجازاتك مو بس الباقي
- **متعلم** (Letta): يتعلم كم مهمة تناسبك وأي وقت أنشط

#### خطة اليوم التلقائية:
```
النظام يجمع من FalkorDB:
1. مواعيد اليوم الثابتة
2. تذكيرات وصل وقتها
3. مهام مشاريع حسب الأولوية
4. ديون قرب استحقاقها
5. مهام مؤجلة من أمس

يرسل عبر تلقرام:
  "صباح الخير أبو ابراهيم!
   1. اجتماع 10:00 — المقاول
   2. كلم أبو محمد (من أمس)
   3. أكمل واجهة المشروع

   ⚡ فاتورة stc بعد 3 أيام
   💰 أبو محمد يطلبك 500 من أسبوعين"
```

#### نهاية اليوم:
- النظام يرسل: "كيف راح يومك؟ خلصت الاجتماع؟"
- يسجل التقدم بناءً على ردك
- اللي ما خلص ينتقل لبكرة أو يتأجل

---

### 6. نظام الأشخاص والعلاقات (People & Relationships)

```
Person:
  - name: string (English)
  - name_ar: string (Arabic)
  - phone: string
  - email: string
  - company: -> Company (optional)
  - relationship: string (friend/colleague/client/contractor/family)
  - notes: [string]

  العلاقات التلقائية (GraphRAG يتتبعها):
  - OWES_MONEY → Debt
  - WORKS_ON → Project
  - PAID_TO ← Expense
  - HAS_REMINDER → Reminder
  - HAS_FILE → File
  - HAS_MEETING → Meeting
```

**"وش القصة مع فلان؟"** → GraphRAG يتتبع كل المسارات ويعطيك الصورة كاملة.

---

### 7. نظام جمع المعرفة (Knowledge Collector)

#### أنواع المعرفة:
```
Knowledge:
  - content: string (English)
  - original_text_ar: string
  - type: string (article/note/experience/howto/reference)
  - source: string (URL, conversation, book...)
  - topic: -> Topic
  - tags: [string]
  - created_at: datetime
```

- **مقال/رابط**: "احفظ لي هالرابط عن..." → يخزن + ملخص
- **ملاحظة**: "معلومة مهمة: ..." → تتخزن وتتربط
- **تجربة**: "جربت كذا وطلع كذا" → learning
- **مرجع**: "لو احتجت أسوي كذا..." → how-to

**البحث**: Qdrant (دلالي) + FalkorDB (علاقات) = نتائج شاملة.

---

## خريطة العلاقات في FalkorDB (Graph Schema)

```
                         ┌──────────┐
                         │  Person  │
                         └────┬─────┘
                              │
         ┌────OWES_MONEY──────┤────WORKS_ON────┐
         │                    │                 │
    ┌────▼────┐         ┌────▼──────┐    ┌────▼────┐
    │  Debt   │         │  Project  │    │ Company │
    └─────────┘         └────┬──────┘    └─────────┘
                             │
              ┌──HAS_TASK────┤───HAS_IDEA───┐
              │              │               │
         ┌────▼───┐    ┌────▼────┐    ┌────▼───┐
         │  Task  │    │  File   │    │  Idea  │
         └────────┘    └─────────┘    └────────┘

    Expense ──PAID_TO──→ Person | Company
    Reminder ──LINKED_TO──→ أي كيان
    Knowledge ──BELONGS_TO──→ Topic
    File ──TAGGED_WITH──→ Tag
    أي كيان ──TAGGED_WITH──→ Tag | Topic
```

#### العلاقات:
| العلاقة | من → إلى | الوصف |
|---------|----------|-------|
| BELONGS_TO | File/Knowledge → Topic | مرتبط بموضوع |
| HAS_TASK | Project → Task | مشروع فيه مهام |
| HAS_IDEA | Project → Idea | فكرة مرتبطة بمشروع |
| OWES_MONEY | Person ↔ You | ديون (direction يحدد الاتجاه) |
| PAID_TO | Expense → Person/Company | مصروف لشخص أو جهة |
| LINKED_TO | Reminder → أي كيان | تذكير مرتبط |
| TAGGED_WITH | أي كيان → Tag | تصنيف |
| WORKS_ON | Person → Project | شخص يشتغل على مشروع |
| HAS_FILE | أي كيان → File | ملف مرتبط |
| SIMILAR_TO | Idea ↔ Idea | أفكار متشابهة (من Qdrant) |

---

## النظام الاستباقي (Proactive System)

### الطبقة 1: الجدول الثابت (Cron via APScheduler)

| الوقت | الحدث | المحتوى |
|-------|-------|---------|
| 07:00 | ملخص الصباح | مواعيد + تذكيرات + مهام معلقة + تنبيهات مالية |
| 13:00 | Check-in الظهر | "كيف ماشي يومك؟" + تذكير بالمهام |
| 21:00 | ملخص المساء | وش خلصت + خطة بكرة |
| الجمعة | ملخص أسبوعي | إنجازات + مشاريع + مالية + خطة الأسبوع الجاي |

### الطبقة 2: المحفزات الذكية (Event Triggers — كل 30 دقيقة)

| الشرط | الإجراء |
|-------|---------|
| دين أقدم من 14 يوم | "فلان يطلبك X من Y يوم، تبي تذكره؟" |
| مشروع متوقف 14+ يوم | "المشروع الفلاني واقف، تبي نشوفه؟" |
| مصاريف تصنيف > المعتاد 40% | "صرفت على أكل برا أكثر من المعتاد" |
| فكرة منسية 30+ يوم | "عندك فكرة عن X ما رجعت لها" |
| تذكير أُجّل 3+ مرات | "هالشي أجلته كثير، تبي تحذفه أو تفوضه؟" |
| موعد بعد ساعة | "عندك موعد بعد ساعة مع فلان" |
| فاتورة متكررة قربت | "فاتورة X بعد 3 أيام" |

### الطبقة 3: التعلم من النمط (Letta Memory)

- يتتبع أوقات نشاطك → يرسل الرسائل بالوقت المناسب
- يتتبع طول الرسائل المفضل → يختصر أو يفصّل
- يتتبع المهام اللي تأجلها → يقترح تقسيمها
- يتتبع المواضيع اللي تهتم فيها → يركز عليها

**كل الرسائل الاستباقية تتولد بـ vLLM** (بالتوازي مع أي طلب ثاني) **وترسل عبر Telegram Bot**.

---

## Pipeline المحادثة التفصيلي

```
رسالة المستخدم (عربي)
        │
        ▼
┌───────────────────────┐
│  Context Builder       │
│  (Python — بدون LLM)  │
│                        │
│  1. آخر 3-5 رسائل     │ ← Redis
│  2. ملخص اليوم         │ ← Redis
│  3. Letta Core Memory  │ ← Redis
│  4. Smart Router:      │
│     سؤال مالي → FalkorDB query
│     بحث غامض → Qdrant search
│     علاقات → FalkorDB GraphRAG
│  5. يرتب الكل في      │
│     prompt (~10K tokens)│
└───────────┬───────────┘
            ▼
       ┌─────────┐
       │  vLLM   │ ← كونتكست مركز ومفيد
       │  32B    │
       └────┬────┘
            ▼
    الرد (بالعربي للمستخدم)
            │
            ▼ (بالتوازي — vLLM concurrent)
┌───────────────────────┐
│  Memory Processor      │
│                        │
│  1. استخراج حقائق JSON │ → FalkorDB
│  2. تحديث ملخص اليوم   │ → Redis
│  3. تحديث Letta Memory │ → Redis
│  4. Contextual chunks   │ → Qdrant
└───────────────────────┘
```

---

## طرق الإدخال

### 1. Telegram Bot (الجوال — أولوية عالية)
- أسرع طريقة: صوت + صور + نص
- **أقل احتكاك = أكثر استخدام (مهم جداً لـ ADHD)**
- ثنائي الاتجاه: يرسل لك ويستقبل ردك
- أزرار سريعة: "خلصت ✓" / "أجل ⏰" / "احذف ✕"

### 2. Open WebUI (الكمبيوتر)
- محادثة + رفع ملفات كبيرة
- مناسب للعمل المطول

### 3. Claude Desktop — MCP Server
- نفس الأدوات من داخل Claude
- مناسب أثناء البرمجة

### 4. واتساب (اختياري — مرحلة متقدمة)
- نفس فكرة التلقرام

---

## مراحل التنفيذ

### المرحلة 1: الأساس + Contextual Retrieval (أسبوع 1-2)
- [ ] إعداد FalkorDB + تصميم الـ Schema
- [ ] إعداد Qdrant + BGE-M3 embeddings
- [ ] إعداد Redis (ملخص اليوم + ذاكرة العمل)
- [ ] RAG API أساسي بـ FastAPI (async)
- [ ] Contextual Retrieval pipeline (إضافة سياق للـ chunks)
- [ ] استراتيجية اللغة الموحدة (ترجمة عند الإدخال/الإخراج)

### المرحلة 2: إدارة الملفات + Agentic RAG (أسبوع 3-4)
- [ ] تصنيف ذكي جديد (برومبتات لكل نوع ملف)
- [ ] Smart Router (يقرر وين يبحث بدون LLM)
- [ ] Agentic RAG (Think → Act → Reflect)
- [ ] Self-RAG (التحقق من النتائج)
- [ ] معالجة: صور (vLLM Vision) + PDF + صوت (WhisperX)

### المرحلة 3: المالية والتذكيرات (أسبوع 5)
- [ ] النظام المالي (مصاريف + ديون + تقارير)
- [ ] نظام التذكيرات الذكية (5 أنواع)
- [ ] استخراج تلقائي من الفواتير (صور + PDF)

### المرحلة 4: المشاريع والتخطيط + GraphRAG (أسبوع 6)
- [ ] إدارة المشاريع والأفكار
- [ ] التخطيط اليومي (ADHD Mode)
- [ ] جمع المعرفة
- [ ] GraphRAG (تتبع العلاقات المعقدة في FalkorDB)

### المرحلة 5: الواجهات + Telegram Bot (أسبوع 7-8)
- [ ] Telegram Bot (ثنائي الاتجاه + أزرار سريعة)
- [ ] Open WebUI Tools (أدوات المحادثة)
- [ ] MCP Server (أدوات Claude Desktop)

### المرحلة 6: النظام الاستباقي + Letta (أسبوع 9-10)
- [ ] Scheduler (ملخصات صباح/ظهر/مساء/أسبوعي)
- [ ] Event Triggers (تنبيهات ذكية كل 30 دقيقة)
- [ ] Letta Memory Layer (التعلم من التفاعلات)
- [ ] ضبط وتحسين بناءً على الاستخدام الفعلي

---

## أمثلة على الاستخدام اليومي

| أنت تقول | النظام يسوي |
|----------|-------------|
| "دفعت 200 لأبو محمد عن غداء" | Expense → FalkorDB + يربطه بأبو محمد |
| *ترفع صورة فاتورة* | vLLM Vision → يستخرج → Expense تلقائي |
| "ذكرني أكلم المقاول السبت" | Reminder → FalkorDB → يذكرك السبت عبر Telegram |
| "رتب لي يومي" | FalkorDB queries → 3-5 مهام مرتبة |
| "مين يطلبني فلوس؟" | FalkorDB → قائمة الديون + المبالغ + المدة |
| "وريني صور رحلة دبي" | Qdrant search + FalkorDB tags → يرسل الصور |
| "عندي فكرة عن..." | يحفظ → Qdrant يكتشف تشابه → يربط بمشروع |
| "وش القصة مع فلان؟" | GraphRAG → كل العلاقات والمسارات |
| "كم صرفت هالشهر؟" | FalkorDB SUM → ملخص + مقارنة |
| *تسجل صوت* | WhisperX → vLLM → يصنف ويتصرف تلقائي |
| "وش أعرف عن IoT؟" | Qdrant + FalkorDB → كل المعرفة المحفوظة |
| "المشروع خلصت 50% منه" | FalkorDB update → يعدل الخطة |
| *النظام يبادر 7 الصبح* | "صباح الخير! عندك 3 أشياء اليوم..." |
| *النظام يبادر — تنبيه* | "فلان يطلبك 500 من 3 أسابيع" |
| *النظام يتعلم* | "لاحظت إنك تفضل 3 مهام بس — خلاص ثبتها" |

---

## ملخص الأدوات النهائي

```
┌──────────────────────────────────────────────────────────┐
│                    المساعد الذكي v2.0                      │
│                                                           │
│   الإدخال          المعالجة              التخزين          │
│   ════════         ════════              ══════           │
│   Telegram    ──→  vLLM (32B)      ──→  FalkorDB         │
│   Open WebUI  ──→  WhisperX        ──→  Qdrant + BGE-M3  │
│   Claude MCP  ──→  FastAPI (async) ──→  Redis            │
│                    APScheduler     ──→  File Storage      │
│                                                           │
│   الأنماط الذكية:                                         │
│   ═══════════════                                        │
│   ✅ Contextual Retrieval (جودة بحث أعلى 49%)            │
│   ✅ Agentic RAG + Self-RAG (يقرر ويصحح نفسه)            │
│   ✅ GraphRAG (علاقات معقدة — دقة 91%)                   │
│   ✅ Letta Memory (يتعلم من المستخدم)                     │
│                                                           │
│   المبادئ:                                                │
│   ════════                                               │
│   • نموذج واحد + vLLM concurrent batching                │
│   • تخزين إنجليزي + ترجمة عند الإدخال/الإخراج            │
│   • كونتكست دايماً < 15K tokens                          │
│   • استباقي: يبادر عبر Telegram                          │
│   • ADHD-friendly: 3-5 مهام، مرن، محفز                  │
└──────────────────────────────────────────────────────────┘
```

---

*تاريخ الإنشاء: 2026-02-09*
*آخر تحديث: 2026-02-10*
*الإصدار: 2.0 — Final*
*مبني على بنية ISE 2026 RAG + أنماط AI حديثة*
